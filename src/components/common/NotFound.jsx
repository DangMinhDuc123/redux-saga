import React from 'react';
import HeaderClient from './Header/index';

const NotFound = () => {
    return (
        <div>
            <HeaderClient />
        </div>
    );
};

export default NotFound;