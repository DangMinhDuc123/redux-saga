import React from 'react';

const MainClient = () => {
    return (
        <div className="places-list">

            {/* {productList.map((product, id) => (
                <div className="places-item">
                    <img src={product.img} alt="description" className="img-places" />
                    <div className="places-body">
                        <h3 className="places-heading">{product.name}</h3>
                        <p className="places-times">{product.Price}</p>
                        <p className="places-desc">{product.description}</p>
                        <a href="!#" className="places-buy-btn">Buy</a>
                    </div>
                </div>
            ))} */}
            Main
        </div>
    );
};

export default MainClient;